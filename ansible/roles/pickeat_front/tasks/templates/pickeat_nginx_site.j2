server {
	listen 80;
	server_name {{ pickeat_site_url }};

	location / {
        try_files $uri $uri/ /index.html;
	}

	location /api {
		rewrite ^/api(.*)$ $1 break;
		proxy_pass http://localhost:8080;
	}
}
